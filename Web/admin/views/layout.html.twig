<!doctype html>
<html class="no-js" lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

        <title></title>
        <meta name="description" content="">
        <meta name="author" content="">

        <meta name="viewport" content="width=device-width">

        <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
        <link rel="stylesheet" href="/bootstrap/css/bootstrap-responsive.min.css">
        <style>
                body {
                    padding-top: 60px;
                    padding-bottom: 40px;
                }
            </style>

        </head>
        <body>
            <div class="navbar  navbar-fixed-top">
                <div class="navbar-inner">
                    <div class="container">
                        <a class="brand" href="#">Silex App</a>

                    {% if app.security.token and is_granted('ROLE_ADMIN')  %}

                        <div class="nav-collapse">
                            <ul class="nav">
                                <li class="active"><a href="/admin">Home</a></li>

                            </ul>
                        </div><!--/.nav-collapse -->

                        <div class="btn-group pull-right">
                            <a class="btn " href="#"><i class="icon-user icon-grey"></i> {{ app.security.token.user.username }}</a>
                            <a class="btn  dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="#"><i class="icon-pencil"></i> Edit</a></li>
                                <li ><a href="{{ url('settings') }}"><i class="icon-wrench"></i> Settings</a></li>
                                <li class="divider"></li>
                                <li><a href="{{ path('logout') }}"><i class="i"></i> Log out</a></li>
                            </ul>

                        </div>


                    {% endif %}
                    </div>
                </div>
            </div>

            <div class="container-fluid">
                <div class="row-fluid">
                      {% if app.security.token and is_granted('ROLE_ADMIN')  %}
                    <div class="span3"> 
            <ul class="nav nav-list">
          
              <li class="nav-header">Application Content</li>
                     {% for value in contentPages %}
      <li><a href="{{url('admin_table_content', {table: value|replace({"content_":""})}) }}">Manage {{value|replace({"_":" "})}} </a> </li>
     {% endfor %}
            
            </ul>
          </div>
              {% endif %} 
                    <div class="span9">
            {% block container_content %}{% endblock %}
                        </div>
  </div>
                  <footer>
                        <p>&copy; Company 2012</p>
                    </footer>
               </div><!-- /container -->
                <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script>
                <script src="/bootstrap/js/bootstrap.min.js"></script>
            </body>
        </html>